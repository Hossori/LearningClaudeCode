[project]
name = "learningclaudecode"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.111.0",
    "jupyter>=1.1.1",
    "python-dotenv>=1.2.2",
    "skills-ref>=0.1.1",
]
